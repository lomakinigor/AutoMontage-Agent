# Intentionally empty: engine renders must never auto-load private .env files.
# Do not add credentials or other values here. REMOTION_* process variables are
# explicitly public and remain available to Remotion's browser environment.
